{
  "schemaVersion": 4,
  "description": "Manifest for the preenfm3 full-render golden-master fixtures. See tests/golden/README.md and _bmad-output/planning-artifacts/golden-master-test-plan.md. Each entry corresponds to one TEST in tests/golden_master_test.cpp. The full render is byte-stable per (libm-class), NOT per compiler: ubuntu gcc and ubuntu clang both link glibc and produce byte-identical renders, while macOS clang links libsystem and differs. The discriminator is the platform/libm; tests/golden_master_test.cpp selects by __APPLE__. FM feedback amplifies the ~1-ULP libm-driven table differences (Osc::init/Env::init precompute via sinf/expf/logf), so cross-libm renders diverge by >1 audio-LSB within ~1 block. v4: compare tolerance is 256 stored units (= 1 audio-LSB; the firmware clamps to 24-bit then <<8), and the hash is diagnostic-only (goldenCompare is authoritative). Fixture bytes are UNCHANGED from v3.",
  "comparison": {
    "artifactType": "int32_t DAC-format render (3 buffers x 64 stereo per block = 192 int32/block); stored value = (24-bit clamped sample) << 8",
    "authoritativeGate": "goldenCompare, ±256 stored int32 units = ±1 audio-LSB (the firmware's final <<8 puts the 24-bit audio in the top 24 bits; a bare ±1 would only cover always-zero padding). Absorbs benign within-platform drift.",
    "hashRole": "DIAGNOSTIC ONLY — FNV-1a 64-bit + splitmix64 over a tolerance-1-normalized buffer; a mismatch is printed to stderr but never fails the test (normalization bucket boundaries can flip under benign drift; making it authoritative would defeat the tolerance). goldenCompare is the sole pass/fail gate.",
    "bufferLayoutPerBlock": "[buffer1(64) buffer2(64) buffer3(64)] = out1/2, out3/4, out5/6",
    "buildFlag": "-ffp-contract=off (tests/CMakeLists.txt) — strict IEEE mul/add, no FMA contraction, for best per-variant reproducibility (does NOT close the cross-libm gap, which is libm-driven)"
  },
  "determinism": {
    "scope": "per (libm-class): macOS-libsystem and linux-glibc renders differ by >1 LSB within ~1 block (libm-driven table differences amplified by FM feedback); ubuntu gcc and ubuntu clang (both glibc) are byte-identical",
    "discriminator": "__APPLE__ (macOS -> _macos variant) vs else (linux/glibc -> _linux variant)",
    "rng": "HAL_RNG gated to seed 0 under PFM3_HOST (Synth.cpp:285); downstream noise[] fill loop shared host/firmware",
    "selfCheck": "GoldenMaster.DeterminismSelfCheck renders twice in-process and asserts byte-exact equality (per-variant)"
  },
  "variants": [
    { "id": "macos", "select": "__APPLE__ defined", "libm": "libsystem", "use": "local dev (macOS Apple clang)" },
    { "id": "linux", "select": "__APPLE__ undefined", "libm": "glibc", "use": "CI ubuntu gcc (Host unit tests) + ubuntu clang (Coverage leg)" }
  ],
  "fixtures": [
    {
      "id": "a4_default_sustain_macos",
      "variant": "macos",
      "preset": "default preenMainPreset (copied by Synth::init into timbre 0)",
      "noteSequence": "noteOn(timbre=0, note=69 [A4], velocity=100) before block 0; no note-off",
      "nBlocks": 200,
      "samplesPerBlock": 192,
      "totalSamples": 38400,
      "hash": "7455c83076f0ffaa",
      "compareLsbTolerance": 256,
      "compareLsbToleranceUnits": "stored int32 (= 1 audio-LSB; firmware clamps 24-bit then <<8)",
      "warmStart": "all 200 blocks captured incl. smoothVolume_/smoothPan_ one-pole transient (~blocks 0-10)",
      "files": ["a4_default_sustain_macos.bin", "a4_default_sustain_macos.xxh", "a4_default_sustain_macos.diff.txt"],
      "guards": "the entire Synth -> Timbre -> Voice -> Matrix -> FxBus -> int32 mix chain on macOS/libsystem"
    },
    {
      "id": "a4_default_sustain_linux",
      "variant": "linux",
      "preset": "default preenMainPreset (copied by Synth::init into timbre 0)",
      "noteSequence": "noteOn(timbre=0, note=69 [A4], velocity=100) before block 0; no note-off",
      "nBlocks": 200,
      "samplesPerBlock": 192,
      "totalSamples": 38400,
      "hash": "8f8766fe3e1ceff3",
      "compareLsbTolerance": 256,
      "compareLsbToleranceUnits": "stored int32 (= 1 audio-LSB; firmware clamps 24-bit then <<8)",
      "warmStart": "all 200 blocks captured incl. smoothVolume_/smoothPan_ one-pole transient (~blocks 0-10)",
      "files": ["a4_default_sustain_linux.bin", "a4_default_sustain_linux.xxh", "a4_default_sustain_linux.diff.txt"],
      "guards": "the entire Synth -> Timbre -> Voice -> Matrix -> FxBus -> int32 mix chain on linux/glibc (CI ubuntu gcc + ubuntu clang-coverage)"
    },
    {
      "id": "envelope_adsr_full_macos",
      "variant": "macos",
      "preset": "default preenMainPreset (timbre 0)",
      "noteSequence": "noteOn(0,69,100)@block0; noteOff(0,69)@block300",
      "nBlocks": 600,
      "samplesPerBlock": 192,
      "totalSamples": 115200,
      "hash": "e873d1dd67184295",
      "compareLsbTolerance": 256,
      "compareLsbToleranceUnits": "stored int32 (= 1 audio-LSB; firmware clamps 24-bit then <<8)",
      "warmStart": "all blocks incl. smoothVolume_/smoothPan_ transient (~blocks 0-10) + Env release tail (blocks 300-599)",
      "files": ["envelope_adsr_full_macos.bin", "envelope_adsr_full_macos.xxh", "envelope_adsr_full_macos.diff.txt"],
      "guards": "Env stage transitions (attack/decay/sustain) + release tail on noteOff — G0's sustain-only golden never exercises noteOff/release"
    },
    {
      "id": "envelope_adsr_full_linux",
      "variant": "linux",
      "preset": "default preenMainPreset (timbre 0)",
      "noteSequence": "noteOn(0,69,100)@block0; noteOff(0,69)@block300",
      "nBlocks": 600,
      "samplesPerBlock": 192,
      "totalSamples": 115200,
      "hash": "0e40387715ce6a5a",
      "compareLsbTolerance": 256,
      "compareLsbToleranceUnits": "stored int32 (= 1 audio-LSB; firmware clamps 24-bit then <<8)",
      "warmStart": "all blocks incl. smoothVolume_/smoothPan_ transient (~blocks 0-10) + Env release tail (blocks 300-599)",
      "files": ["envelope_adsr_full_linux.bin", "envelope_adsr_full_linux.xxh", "envelope_adsr_full_linux.diff.txt"],
      "guards": "Env stage transitions (attack/decay/sustain) + release tail on noteOff — G0's sustain-only golden never exercises noteOff/release"
    },
    {
      "id": "fm_algo2_macos",
      "variant": "macos",
      "preset": "default preenMainPreset, timbre 0 engine1.algo overridden to ALGO2",
      "noteSequence": "setTimbreAlgo(0, ALGO2) + afterNewParamsLoad(0); noteOn(0,69,100)@block0; no note-off",
      "nBlocks": 200,
      "samplesPerBlock": 192,
      "totalSamples": 38400,
      "hash": "713eab8fc66121f7",
      "compareLsbTolerance": 256,
      "compareLsbToleranceUnits": "stored int32 (= 1 audio-LSB; firmware clamps 24-bit then <<8)",
      "warmStart": "all 200 blocks incl. smoothVolume_/smoothPan_ transient (~blocks 0-10)",
      "files": ["fm_algo2_macos.bin", "fm_algo2_macos.xxh", "fm_algo2_macos.diff.txt"],
      "guards": "ALGO2 ({1,1,2,0,0,0}) = 2 carriers + 1 modulator — the multi-carrier output summing path the default ALGO1 (1 carrier) never exercises"
    },
    {
      "id": "fm_algo2_linux",
      "variant": "linux",
      "preset": "default preenMainPreset, timbre 0 engine1.algo overridden to ALGO2",
      "noteSequence": "setTimbreAlgo(0, ALGO2) + afterNewParamsLoad(0); noteOn(0,69,100)@block0; no note-off",
      "nBlocks": 200,
      "samplesPerBlock": 192,
      "totalSamples": 38400,
      "hash": "e6eb29e6882e3e37",
      "compareLsbTolerance": 256,
      "compareLsbToleranceUnits": "stored int32 (= 1 audio-LSB; firmware clamps 24-bit then <<8)",
      "warmStart": "all 200 blocks incl. smoothVolume_/smoothPan_ transient (~blocks 0-10)",
      "files": ["fm_algo2_linux.bin", "fm_algo2_linux.xxh", "fm_algo2_linux.diff.txt"],
      "guards": "ALGO2 ({1,1,2,0,0,0}) = 2 carriers + 1 modulator — the multi-carrier output summing path the default ALGO1 (1 carrier) never exercises"
    },
    {
      "id": "fm_algo27_6carrier_macos",
      "variant": "macos",
      "preset": "default preenMainPreset, timbre 0 engine1.algo overridden to ALG27",
      "noteSequence": "setTimbreAlgo(0, ALG27) + afterNewParamsLoad(0); noteOn(0,69,100)@block0; no note-off",
      "nBlocks": 200,
      "samplesPerBlock": 192,
      "totalSamples": 38400,
      "hash": "686bac6da3937528",
      "compareLsbTolerance": 256,
      "compareLsbToleranceUnits": "stored int32 (= 1 audio-LSB; firmware clamps 24-bit then <<8)",
      "warmStart": "all 200 blocks incl. smoothVolume_/smoothPan_ transient (~blocks 0-10)",
      "files": ["fm_algo27_6carrier_macos.bin", "fm_algo27_6carrier_macos.xxh", "fm_algo27_6carrier_macos.diff.txt"],
      "guards": "ALG27 ({1,1,1,1,1,1}) = all 6 carriers, additive — the maximal carrier-topology departure from ALGO1; exercises the full 6-carrier summing path. (Chosen over ALG17, which renders byte-identical to G0 under the zero-IM default preset — see spec Design Notes.)"
    },
    {
      "id": "fm_algo27_6carrier_linux",
      "variant": "linux",
      "preset": "default preenMainPreset, timbre 0 engine1.algo overridden to ALG27",
      "noteSequence": "setTimbreAlgo(0, ALG27) + afterNewParamsLoad(0); noteOn(0,69,100)@block0; no note-off",
      "nBlocks": 200,
      "samplesPerBlock": 192,
      "totalSamples": 38400,
      "hash": "0814ade1448fb2bb",
      "compareLsbTolerance": 256,
      "compareLsbToleranceUnits": "stored int32 (= 1 audio-LSB; firmware clamps 24-bit then <<8)",
      "warmStart": "all 200 blocks incl. smoothVolume_/smoothPan_ transient (~blocks 0-10)",
      "files": ["fm_algo27_6carrier_linux.bin", "fm_algo27_6carrier_linux.xxh", "fm_algo27_6carrier_linux.diff.txt"],
      "guards": "ALG27 ({1,1,1,1,1,1}) = all 6 carriers, additive — the maximal carrier-topology departure from ALGO1; exercises the full 6-carrier summing path. (Chosen over ALG17, which renders byte-identical to G0 under the zero-IM default preset — see spec Design Notes.)"
    },
    {
      "id": "multi_timbre_mix_macos",
      "variant": "macos",
      "preset": "default preenMainPreset (timbres 0 + 1)",
      "noteSequence": "noteOn(0,69,100) + noteOn(1,72,100)@block0; no note-off",
      "nBlocks": 200,
      "samplesPerBlock": 192,
      "totalSamples": 38400,
      "hash": "d8f72fbd66ea49bf",
      "compareLsbTolerance": 256,
      "compareLsbToleranceUnits": "stored int32 (= 1 audio-LSB; firmware clamps 24-bit then <<8)",
      "warmStart": "all 200 blocks incl. per-timbre smoothVolume_/smoothPan_ transient (~blocks 0-10)",
      "files": ["multi_timbre_mix_macos.bin", "multi_timbre_mix_macos.xxh", "multi_timbre_mix_macos.diff.txt"],
      "guards": "voicesToTimbre multi-timbre mix + per-timbre smoothVolume_/smoothPan_ + fxBus->mixAdd — timbres 0+1 (6+6=12 <= MAX_NUMBER_OF_VOICES 16) summing into the 6 outputs"
    },
    {
      "id": "multi_timbre_mix_linux",
      "variant": "linux",
      "preset": "default preenMainPreset (timbres 0 + 1)",
      "noteSequence": "noteOn(0,69,100) + noteOn(1,72,100)@block0; no note-off",
      "nBlocks": 200,
      "samplesPerBlock": 192,
      "totalSamples": 38400,
      "hash": "48ad9f3e5afc2d0c",
      "compareLsbTolerance": 256,
      "compareLsbToleranceUnits": "stored int32 (= 1 audio-LSB; firmware clamps 24-bit then <<8)",
      "warmStart": "all 200 blocks incl. per-timbre smoothVolume_/smoothPan_ transient (~blocks 0-10)",
      "files": ["multi_timbre_mix_linux.bin", "multi_timbre_mix_linux.xxh", "multi_timbre_mix_linux.diff.txt"],
      "guards": "voicesToTimbre multi-timbre mix + per-timbre smoothVolume_/smoothPan_ + fxBus->mixAdd — timbres 0+1 (6+6=12 <= MAX_NUMBER_OF_VOICES 16) summing into the 6 outputs"
    }
  ]
}
